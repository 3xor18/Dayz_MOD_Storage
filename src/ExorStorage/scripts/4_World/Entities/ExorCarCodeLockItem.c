// ============================================================================
// 3xorStorage - ITEM keypad del candado de autos
// ----------------------------------------------------------------------------
// Item de inventario. Se lleva en la mano y con "Poner/Cambiar clave" mirando un auto
// del clan se coloca el candado (ver ExorActionSetCarKey). La clase de config esta en
// config.cpp (modelo placeholder por ahora); esta declaracion de SCRIPT es la que hace
// que IsInherited(Exor_CarCodeLock) funcione en las acciones.
// ============================================================================
class Exor_CarCodeLock : ItemBase
{
}
